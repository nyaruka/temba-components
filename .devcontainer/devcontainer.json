{
	"name": "temba-components",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"forwardPorts": [
		3010
	],
	"remoteUser": "node",
	"postCreateCommand": "sudo chown node /workspaces/temba-components && ls -l .. && whoami && pwd && ls -l && yarn install"
	// "features": {},
	// "customizations": {},
}