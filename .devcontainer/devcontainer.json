{
  "name": "temba-components-${localWorkspaceFolderBasename}",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {},
  "forwardPorts": [
    3010
  ],
  "runArgs": [
    "--network=textit_default",
    "--hostname=temba-components",
    "--add-host=minio:host-gateway"
  ],
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/${localWorkspaceFolderBasename},type=bind,consistency=cached",
  "remoteUser": "node",
  "postCreateCommand": "sudo chown -R node /workspaces/${localWorkspaceFolderBasename}/static/svg && sudo chown node /workspaces/${localWorkspaceFolderBasename} && sudo chown node /workspaces/${localWorkspaceFolderBasename}/screenshots && yarn install && echo 'Testing minio connection:' && curl -f http://minio:9000/minio/health/live || echo 'Minio connection failed - this is expected if minio is not running'",
  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh",
        "editor.formatOnSave": true,
        "editor.rulers": [
          120
        ],
        "prettier": {
          "configPath": ".prettierrc",
          "documentSelectors": [
            "*.{ts,js}"
          ]
        }
      }
    }
  }
}